#!/usr/bin/env sh
set -e

echo "Checking formatting..."
pnpm format:check

echo "Running Nx affected checks..."
pnpm nx affected -t lint,typecheck,test --uncommitted --parallel=10

echo ""
echo "✅ Pre-commit checks passed"
