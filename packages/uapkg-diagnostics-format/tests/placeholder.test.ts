import { describe, expect, it } from 'vite-plus/test';

describe('placeholder', () => {
  it('keeps vitest from failing when no package tests exist yet', () => {
    expect(true).toBe(true);
  });
});
