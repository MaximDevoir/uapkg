/**
 * Diagnostic severity levels.
 *
 * - `error`   – operation cannot continue
 * - `warning` – operation can continue but something is concerning
 * - `info`    – informational only
 */
export type DiagnosticLevel = 'error' | 'warning' | 'info';
